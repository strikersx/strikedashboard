# Dependencies
node_modules

# Build output
.next
out

# Test coverage (root-only — don't accidentally ignore feature dirs named "coverage")
/coverage

# Environment variables
.env
.env.local

# Prisma generated
/generated/prisma

# SQLite databases
*.db
*.db-journal

# Lead export CSVs (contain PII: names + phone numbers)
leads-*.csv

# TypeScript
*.tsbuildinfo
next-env.d.ts
.vercel

# Pre-apply schema dumps (may contain schema details; never commit)
backups/
